{{!-- header for homepage --}}
<div>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">Only Recipes</a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="nav navbar-nav ml-auto ">
            {{!-- display logout only if logged in --}}
            {{#if loggedIn}}
                <li class="nav-item">
                    <a id="logout"class="nav-link">Logout</a>
                </li>
            {{!-- prompt user to login or create account --}}
            {{else}}
                <li class="nav-item">
                    <a class="nav-link" href="/login">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/signup">Signup</a>
                </li>
            {{/if}}
            </ul>
        </div>
    </nav>
</div>

{{!-- show in profile --}}
{{#if loggedIn}}
    {{!-- TODO: create partial to render profile data  --}}
   
    {{> profile-details}}

    {{!-- add in logout functionality --}}
    <script src="/js/logout.js"></script>
{{!-- shows default landing page image --}}
{{else}}
    <img src="./images/background.png" style="width: 100%; height: auto;margin: 0;">
{{/if}}
